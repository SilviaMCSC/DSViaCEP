export default function Address (cep, street, number, city){
this.cep = cep;
this.street = street;
this.number = number;
this.city = city;
}
//subpasta para criaçao do tipo do objeto do meu formulário, que no caso é o endereço. 
//Mostra o tipo do meu enderço
//função que representa dos dados.